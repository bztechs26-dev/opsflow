# Lambda code

`src/handler.py` is the AWS Lambda entry point (`handler.handler`).

The source has no third-party dependencies. The AWS Lambda Python runtime supplies `boto3` for the infrastructure-only folder and notification setup.
