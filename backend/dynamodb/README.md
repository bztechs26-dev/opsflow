# DynamoDB

This folder will hold OpsFlow DynamoDB repository and persistence code after the import-file format and item schema are agreed.

The existing `ops-flow-valassis` table is intentionally untouched by the backend foundation. No DynamoDB reads or writes are performed yet.
